#include "main.h"
#include <stdio.h>
#include <string.h>
 /** 
  *_strcat - a function that concatenates two strings 
  *@dest: copy to 
  *@src: copy from 
  *Return: two concat strings 
  */
char *_strcat(char *dest, char *src);
char destination[] = "Hello ";
char source[] = "World!";
printf("concentrated srting: %s\n", strcat(destination, source));
return (0);
}

